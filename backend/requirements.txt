# Core pipeline
google-cloud-storage>=2.14.0
google-cloud-aiplatform[agent_engines]>=1.60.0
google-cloud-discoveryengine>=0.13.0
google-cloud-firestore>=2.14.0
google-cloud-bigquery>=3.18.0
google-genai>=1.0.0
tiktoken>=0.7.0
python-dotenv>=1.0.0

# Web crawling
firecrawl-py>=1.0.0
requests>=2.31.0        # gov-news RSS poll (scripts/curation/poll_gov_news.py) — previously only a transitive dep
beautifulsoup4>=4.12.0  # gov-news full-article-text fallback (GOV-NEWS-INGESTION-PLAN.md §3.5)

# API server
fastapi>=0.110.0
uvicorn>=0.27.0
firebase-admin>=6.5.0   # server-side Firebase ID-token verification (auth gate)
resend>=2.0.0           # email verification codes
